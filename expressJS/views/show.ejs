<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Time sheet list</title>
    <link rel="stylesheet" href="/expressJS/css/index.css">
    <style>
        #table_list{
            /* margin-left: 18%; */
            /* margin-top: 8%; */
            font-size: 25px;
            background-color: whitesmoke;
            width: 75vw;
            height: 35vw;
            margin-top: 5%;
            margin-left: 13%;
            border-radius: 25px;
        }
        #text{
            margin-left: 28%;
            margin-bottom: 5%;
        }
        #add_btn{
            background-color:rgb(26, 219, 219);
            height: 2vw;
            font-size: 1.5vw;
            border-radius: 10px;
            padding-left: 72px;
            padding-right: 72px;
            padding-top: 5px;
            padding-bottom: 5px;
            margin-top: 4%;
            color: rgb(1, 55, 55);
            text-decoration: none;
        }
    </style>
</head>
<body>

    <div id="table_list">
        <a href="/add" id="add_btn">Add</a>
        <h1 id="text">Time Sheet List</h1>
         <table border="1">
            <tr>
                <th>S.N</th>
                <th>user name</th>
                <th>Date of the task</th>
                <th>Task name</th>
                <th>How many hours ?</th>
                <th>Delete</th>
            </tr>
            
            <% if(userData.length!=0){ %>
                <!-- <% for (i = 0; i < userData.length; i++) { %> 
                
                
                <% } %> -->
                <% for (let data of userData) { %>
                    <tr>
                        <td><%=data.id%></td>
                        <td><%=data.userName%></td>
                        <td><%=data.date%></td>
                        <td><%=data.no_of_hours%></td>
                        <td><%=data.task_name%></td>
                        <td><a href="/delete/<%=data.id%>">Delete</a></td>
                    </tr>
                    <% }%>
            <% } else{ %>
                <tr>
                    <td colspan="7">No Data Found</td>
                </tr>
            <% } %> 
        </table>
    </div> 

</body>
</html>